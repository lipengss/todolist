# FocusWorkspace 生产环境配置

# JWT 签名密钥（务必改成随机字符串！建议用 `openssl rand -hex 32` 生成）
JWT_SECRET=change-me-to-a-random-string

# 默认管理员账号（首次启动时自动创建，后续可删除此配置）
AUTH_USERNAME=admin
AUTH_PASSWORD=admin

# 访问端口（默认 80，如需改端口设这个）
PORT=80
